import logging
from pistarlab import ctx

PLUGIN_ID="$id"
PLUGIN_VERSION="$version"

def manifest():
    return {}

def install():
    # Uncomment if using manifest
    # ctx.install_plugin_from_manifest(PLUGIN_ID)

    return True

def load():
    return True

def uninstall():
    logging.info("Uninstalling {}".format(PLUGIN_ID))
    ctx.disable_plugin_by_id(PLUGIN_ID)
    return True